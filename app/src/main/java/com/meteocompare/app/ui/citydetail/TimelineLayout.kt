package com.meteocompare.app.ui.citydetail

/** Présentation visuelle de la section Chronologie dans la page détail. */
internal enum class TimelineLayout {
    /** Cartes verticales successives, présentation historique de l'application. */
    COLUMNS,

    /** Frise synchronisée par métrique, inspirée de la vue détail web. */
    CHRONO
}
