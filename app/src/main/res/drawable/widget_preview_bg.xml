<?xml version="1.0" encoding="utf-8"?>
<!--
    Fond du widget_preview.xml — rendu dans le picker de widgets.

    Radius 16dp aligné sur la taille de coin standard des widgets modernes
    (Material 3 / Android 12+). Couleur : ?android:attr/colorBackground
    qui suit le thème système clair/sombre → l'aperçu s'adapte au device
    de l'utilisateur.

    Bord subtil de 1dp à faible alpha pour délimiter visuellement l'aperçu
    du fond du picker (parfois presque identique en couleur, sinon on ne
    voit plus les contours du widget).
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="?android:attr/colorBackground" />
    <corners android:radius="16dp" />
    <stroke
        android:width="1dp"
        android:color="#1A000000" />
</shape>
